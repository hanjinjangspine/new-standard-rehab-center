name: Notify IndexNow after production deploy

on:
  deployment_status:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Validate the live key, robots.txt, and sitemap without submitting
        required: true
        type: boolean
        default: true

permissions:
  contents: read

concurrency:
  group: indexnow-rehab-production-${{ github.event.deployment.sha || github.sha }}
  cancel-in-progress: false

jobs:
  notify-indexnow:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.deployment_status.state == 'success' &&
       github.event.deployment.environment == 'Production' &&
       github.event.deployment.creator.login == 'vercel[bot]')
    runs-on: ubuntu-latest

    steps:
      - name: Check out deployed revision
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.deployment.sha || github.sha }}
          fetch-depth: 2

      - name: Use Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false

      - name: Confirm production revision
        id: production
        shell: bash
        env:
          DEPLOY_SHA: ${{ github.event.deployment.sha || github.sha }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            echo "should_submit=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git fetch --no-tags --depth=50 origin main:refs/remotes/origin/main
          if [[ "$DEPLOY_SHA" == "$(git rev-parse origin/main)" ]]; then
            echo "should_submit=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_submit=false" >> "$GITHUB_OUTPUT"
            echo "The successful deployment is not the current main revision; IndexNow was skipped."
          fi

      - name: Test IndexNow automation
        if: steps.production.outputs.should_submit == 'true'
        run: node --test scripts/indexnow.test.mjs

      - name: Verify canonical production site
        if: steps.production.outputs.should_submit == 'true'
        run: node scripts/verify-production.mjs --origin https://rehab.new-standard.co.kr

      - name: Notify IndexNow
        if: steps.production.outputs.should_submit == 'true'
        shell: bash
        env:
          DRY_RUN: ${{ inputs.dry_run || false }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          args=()
          if [[ "$EVENT_NAME" == "workflow_dispatch" && "$DRY_RUN" == "true" ]]; then
            args+=(--dry-run)
          fi
          node scripts/submit-indexnow.mjs "${args[@]}"
